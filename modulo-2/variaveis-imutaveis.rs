/*
Variaveis Imutáveis:
No Rust, as variáveis são imutáveis por padrão. 
Isso significa que, uma vez que um valor é atribuído a uma variável, ele não pode ser alterado.
*/

fn main () {
    let pontos = 0;
    pontos = 10; //ERRO! Você não pode mudar x!
}