use std::io;

fn main() {
    println!("=== CALCULADORA RUST ===");

    // TODO 1: Pergunte o nome do usuário, leia a entrada do teclado
    // e salve em uma variável (lembre-se de limpar os espaços/enter com trim)

    // TODO 2: Peça para o usuário digitar o PRIMEIRO número.
    // Leia a entrada e converta para o tipo decimal (f64) usando parse().unwrap()

    // TODO 3: Peça para o usuário digitar o SEGUNDO número.
    // Leia a entrada e converta para o tipo decimal (f64)

    // TODO 4: Imprima na tela os resultados das 4 operações matemáticas básicas:
    // - Soma (+)
    // - Subtração (-)
    // - Multiplicação (*)
    // - Divisão (/)
}
